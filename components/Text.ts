import styled from 'styled-components';
import { Colors } from '../constants';

export const Text = styled.p`
  font-size: 14px;
  color: ${Colors.HEADER};
  font-weight: normal;
  text-align: center;
  margin: 0;
`;
