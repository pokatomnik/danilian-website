import styled from 'styled-components';
import { Colors } from '../constants';

export const Header = styled.h1`
  font-size: 24px;
  color: ${Colors.HEADER};
  text-align: center;
  font-weight: bold;
`;
